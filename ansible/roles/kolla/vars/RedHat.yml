---
# List of packages to install.
kolla_package_dependencies:
  - "{{ 'docker-buildx-plugin' if container_engine == 'docker' }}"
  - gcc
  - git
  - libffi-devel
  - openssl-devel
  - python3-devel
  - python3-pip
