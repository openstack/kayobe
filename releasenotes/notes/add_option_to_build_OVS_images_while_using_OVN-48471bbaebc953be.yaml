---
features:
  - |
    Adds a new variable ``kolla_build_neutron_ovs`` which gives users the
    option to build Neutron OVS container images while the system is using OVN.
    This is useful when users want to build all Neutron container images at the
    same time.
