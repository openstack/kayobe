---
{% if infra_vm_use_cirros | default(true) or ansible_os_family != "Debian" %}
aio_interface: eth0
{% else %}
# Required for Ubuntu cloud images
aio_interface: ens2
{% endif %}

# Route via the seed-hypervisor to the outside world.
aio_gateway: 192.168.33.4
